// This script is intentionally left simple to preserve the original static layout.
// Database jobs are handled on the main job board.
document.addEventListener("DOMContentLoaded", () => {
    console.log("Category page loaded in static mode.");
});
